<!DOCTYPE html> <html> <head>
    <title>{{ page_title }}</title>
</head>
<body>
    <h1>{{ page_title }}</h1>
    <p>Це приклад тексту з макету Figma.</p>

    <button onclick="alert('Кнопку натиснули!')">Натисни мене</button>

    <div style="display: flex; gap: 10px; margin-top: 20px;">
        {% for card in cards %}
        <div style="border: 1px solid #ccc; padding: 10px; width: 150px; text-align: center;">
            <img src="{{ card.image }}" alt="{{ card.title }}" width="150">
            <h3>{{ card.title }}</h3>
        </div>
        {% endfor %}
    </div>
</body>
</html>
